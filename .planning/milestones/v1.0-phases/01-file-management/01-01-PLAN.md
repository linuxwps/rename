---
phase: 01-file-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rname/package.json
  - rname/src-tauri/Cargo.toml
  - rname/src-tauri/tauri.conf.json
  - rname/src-tauri/capabilities/main.json
  - rname/src/main.tsx
  - rname/src/App.tsx
  - rname/src/App.css
  - rname/src/components/FileDropZone.tsx
  - rname/src/components/FileList.tsx
  - rname/src/components/FileListRow.tsx
  - rname/src/hooks/useFileList.ts
  - rname/src/types/file.ts
  - rname/src/utils/formatFileSize.ts
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-03
  - FILE-04
  - FILE-05
---

<objective>
Initialize Tauri project and implement core file management: drag-drop zone, file picker, file list display with metadata, and remove/clear functionality.
</objective>

<context>
@.planning/phases/01-file-management/01-CONTEXT.md
@.planning/phases/01-file-management/01-RESEARCH.md
@.planning/phases/01-file-management/SKELETON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize Tauri v2 project with React + TypeScript</name>
  <files>rname/package.json, rname/src-tauri/Cargo.toml, rname/src-tauri/tauri.conf.json</files>
  <read_first>
    - /Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/01-file-management/01-RESEARCH.md (standard stack, installation commands)
    - /Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/01-file-management/SKELETON.md (project structure)
  </read_first>
  <action>
    Initialize new Tauri project with React TypeScript template. Run:
    ```bash
    cd /Users/linuxwps/Downloads/OpenCode/Rname
    npm create tauri-app@latest rname -- --template react-ts
    cd rname
    npm install
    npm install @tauri-apps/plugin-dialog @tauri-apps/plugin-fs @tauri-apps/plugin-os
    ```

    Verify project compiles with `npm run tauri build -- --debug` or at minimum `npm run dev` starts without errors.
  </action>
  <behavior>
    - Test: npm run dev launches Tauri dev server and opens app window
    - Test: No console errors on startup
  </behavior>
  <verify>
    <automated>npm run build -- --debug 2>&1 | head -50 || npm run dev -- --port 5173 2>&1 | head -20</automated>
  </verify>
  <done>Tauri project compiles and launches window successfully</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tauri capabilities for file system and dialog access</name>
  <files>rname/src-tauri/capabilities/main.json</files>
  <read_first>
    - /Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/01-file-management/01-RESEARCH.md (Pitfall 1: permissions)
    - rname/src-tauri/tauri.conf.json (existing config if any)
  </read_first>
  <action>
    Create capabilities configuration file at src-tauri/capabilities/main.json with:
    - core:default
    - dialog:default, dialog:allow-open
    - fs:default, fs:allow-stat, fs:allow-read-dir
    - os:default (for platform detection)

    Also verify src-tauri/tauri.conf.json has app identifier and window config for macOS.
  </action>
  <verify>
    <automated>cat rname/src-tauri/capabilities/main.json | grep -E '"dialog"|"fs:"' | wc -l</automated>
  </verify>
  <done>Capabilities file exists with all required permissions for file operations</done>
</task>

<task type="auto">
  <name>Task 3: Implement file list types and utilities</name>
  <files>rname/src/types/file.ts, rname/src/utils/formatFileSize.ts</files>
  <read_first>
    - /Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/01-file-management/01-RESEARCH.md (Pattern 4: FileItem interface)
  </read_first>
  <action>
    Create types/file.ts with FileItem interface containing:
    - id: string (unique identifier)
    - path: string (full file path)
    - name: string (filename without path)
    - extension: string (without dot)
    - size: number (bytes)
    - modifiedAt: number (Unix timestamp)

    Create utils/formatFileSize.ts with formatFileSize(bytes: number): string function that returns human-readable size (B, KB, MB, GB).
  </action>
  <verify>
    <automated>ls -la rname/src/types/file.ts rname/src/utils/formatFileSize.ts</automated>
  </verify>
  <done>Type definitions and utilities created with exports</done>
</task>

<task type="auto">
  <name>Task 4: Implement useFileList hook with drag-drop, picker, add/remove/clear</name>
  <files>rname/src/hooks/useFileList.ts</files>
  <read_first>
    - /Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/01-file-management/01-RESEARCH.md (Pattern 4: complete hook example, lines 391-542)
    - rname/src/types/file.ts (from Task 3)
  </read_first>
  <action>
    Implement useFileList hook with:
    - State: files (FileItem[]), isDragging (boolean)
    - addFiles(paths: string[]): fetches metadata via stat(), enforces 100 file limit (D-06)
    - removeFile(fileId: string): filters out single file (FILE-04)
    - clearFiles(): resets to empty array (FILE-05)
    - openFilePicker(): uses @tauri-apps/plugin-dialog open() with multi-select and filters (D-03)
    - openFolderPicker(): uses open() with directory:true, then readDir to get file paths
    - Drag-drop listener via appWindow.onFileDropEvent (FILE-01)
    - Filter options per D-03: Images, Documents, Video, Audio, All Files
  </action>
  <verify>
    <automated>grep -l "openFilePicker\|removeFile\|clearFiles\|onFileDropEvent" rname/src/hooks/useFileList.ts</automated>
  </verify>
  <done>useFileList hook implements all 5 requirements (FILE-01 through FILE-05)</done>
</task>

<task type="auto">
  <name>Task 5: Implement FileDropZone component with visual feedback</name>
  <files>rname/src/components/FileDropZone.tsx</files>
  <read_first>
    - /Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/01-file-management/01-RESEARCH.md (Pattern: visual feedback, lines 544-584)
    - /Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/01-file-management/01-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    Create FileDropZone component:
    - Width: 33.33% of container (D-04: left 1/3 area)
    - Shows "拖入文件到这里" hint text when empty (D-05)
    - Visual feedback: border highlight, background color change on drag hover (isDragging state)
    - Contains "选择文件" and "选择文件夹" buttons that call openFilePicker/openFolderPicker
    - Displays FileList component when files exist
  </action>
  <verify>
    <automated>grep -E "33.33%|拖入文件到这里|isDragging" rname/src/components/FileDropZone.tsx</automated>
  </verify>
  <done>FileDropZone implements D-04 (left 1/3 area) and D-05 (visual feedback)</done>
</task>

<task type="auto">
  <name>Task 6: Implement FileList and FileListRow components</name>
  <files>rname/src/components/FileList.tsx, rname/src/components/FileListRow.tsx</files>
  <read_first>
    - rname/src/hooks/useFileList.ts (from Task 4)
    - rname/src/types/file.ts (from Task 3)
    - rname/src/utils/formatFileSize.ts (from Task 3)
  </read_first>
  <action>
    Create FileListRow component that renders single file row with:
    - Columns: 文件名, 扩展名, 大小, 修改日期 (FILE-03)
    - Remove button per row (calls removeFile for FILE-04)
    - Uses formatFileSize for display

    Create FileList component that:
    - Maps over files array
    - Renders table header with column labels
    - Renders FileListRow for each file
    - Includes "清空列表" button at bottom (calls clearFiles for FILE-05)
  </action>
  <verify>
    <automated>grep -E "文件名|扩展名|大小|修改日期|清空列表" rname/src/components/FileList.tsx</automated>
  </verify>
  <done>FileList displays all metadata columns and provides remove/clear functionality</done>
</task>

<task type="auto">
  <name>Task 7: Wire components in App.tsx</name>
  <files>rname/src/App.tsx, rname/src/App.css</files>
  <read_first>
    - rname/src/components/FileDropZone.tsx (from Task 5)
    - rname/src/hooks/useFileList.ts (from Task 4)
  </read_first>
  <action>
    Update App.tsx to:
    - Import useFileList hook
    - Render FileDropZone component (takes left 1/3)
    - Placeholder area for right 2/3 (will be used in Phase 2)
    - Use inline styles or App.css for layout: display flex, left panel 33.33%, right panel 66.67%

    Update App.css with basic styles for:
    - Table layout for file list
    - Drop zone visual states (normal, dragging)
    - Button styles
  </action>
  <verify>
  <automated>grep -E "FileDropZone|33.33%|66.67%" rname/src/App.tsx</automated>
  </verify>
  <done>App.tsx renders complete layout with file management functionality</done>
</task>

</tasks>

<must_haves>
  truths:
    - "User can drag files from Finder into the app"
    - "User can select files via native file picker dialog"
    - "File list displays filename, extension, size, and modified date"
    - "User can remove a single file from the list"
    - "User can clear all files from the list"
  artifacts:
    - path: "rname/src/hooks/useFileList.ts"
      provides: "Core state management for file operations"
      exports: "addFiles, removeFile, clearFiles, openFilePicker, openFolderPicker, files, isDragging"
    - path: "rname/src/components/FileDropZone.tsx"
      provides: "Left panel with drop zone and buttons"
      contains: "Drag area, visual feedback, picker buttons"
    - path: "rname/src/components/FileList.tsx"
      provides: "File metadata display table"
      contains: "Filename, extension, size, modified date columns"
    - path: "rname/src-tauri/capabilities/main.json"
      provides: "Tauri permissions for file operations"
      contains: "dialog and fs plugin permissions"
  key_links:
    - from: "rname/src/components/FileDropZone.tsx"
      to: "rname/src/hooks/useFileList.ts"
      via: "useFileList() hook"
      pattern: "const { files, isDragging, openFilePicker, openFolderPicker } = useFileList()"
    - from: "rname/src/hooks/useFileList.ts"
      to: "@tauri-apps/plugin-dialog"
      via: "open() function"
      pattern: "import \\{ open \\} from '@tauri-apps/plugin-dialog'"
    - from: "rname/src/hooks/useFileList.ts"
      to: "@tauri-apps/plugin-fs"
      via: "stat() function"
      pattern: "import \\{ stat, readDir \\} from '@tauri-apps/plugin-fs'"
    - from: "rname/src/components/FileList.tsx"
      to: "rname/src/hooks/useFileList.ts"
      via: "removeFile, clearFiles"
      pattern: "onRemove=\\{removeFile\\}|onClear=\\{clearFiles\\}"
</must_haves>

<verification>
- Run `npm run tauri dev` and verify app launches
- Test drag-drop by dragging files onto left panel
- Test file picker by clicking "选择文件" button
- Verify file list displays all 4 columns (name, extension, size, date)
- Test remove button removes single file
- Test "清空列表" button clears all files
</verification>

<success_criteria>
- All 5 requirements (FILE-01 through FILE-05) are functional
- App runs without errors in dev mode
- File list shows correct metadata for selected files
- Drag-drop, picker, remove, and clear all work as expected
</success_criteria>

<output>
After completion, create `.planning/phases/01-file-management/01-01-SUMMARY.md`