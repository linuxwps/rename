---
phase: 02-rename-preview
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - rname/src/components/rename/DiffCell.tsx
  - rname/src/components/rename/DiffCell.css
  - rname/src/components/rename/ConflictBanner.tsx
  - rname/src/components/rename/ConflictBanner.css
  - rname/src/components/rename/RenameTabBar.tsx
  - rname/src/components/rename/RenameTabBar.css
  - rname/src/components/rename/RenamePanel.tsx
  - rname/src/components/rename/RenamePanel.css
autonomous: true
requirements:
  - PREV-02
  - PREV-03
user_setup: []

must_haves:
  truths:
    - "新文件名中的变化部分以绿色（新增）/红色（删除）背景高亮显示"
    - "扩展名和文件名的变化分别高亮"
    - "冲突行在文件名前显示 ⚠ 图标并添加红色边框"
    - "顶部 Tab 栏有 6 个可切换的标签（时间/正则/前缀/后缀/扩展名/替换）"
    - "冲突时顶部横幅显示 '发现 N 个重名文件'"
    - "无文件时右侧面板显示 '请先在左侧添加文件'"
  artifacts:
    - path: "rname/src/components/rename/DiffCell.tsx"
      provides: "高亮差异单元格组件，渲染 diffBaseName + diffExtension"
      contains: "export function DiffCell"
    - path: "rname/src/components/rename/ConflictBanner.tsx"
      provides: "顶部冲突计数横幅"
      contains: "export function ConflictBanner"
    - path: "rname/src/components/rename/RenameTabBar.tsx"
      provides: "6 标签多选 Tab 栏"
      contains: "export function RenameTabBar"
    - path: "rname/src/components/rename/RenamePanel.tsx"
      provides: "右侧面板容器，集成 TabBar + 表单区域 + Banner"
      contains: "export function RenamePanel"
  key_links:
    - from: "DiffCell.tsx"
      to: "types/rename.ts"
      via: "导入 DiffSegment 类型"
      pattern: "DiffSegment"
    - from: "RenameTabBar.tsx"
      to: "types/rename.ts"
      via: "导入 RenameModeStates 类型"
      pattern: "RenameModeStates"
    - from: "RenamePanel.tsx"
      to: "RenameTabBar, ConflictBanner"
      via: "导入并组合子组件"
---

<objective>
**目标：** 创建核心 UI 组件 — 差异高亮单元格（DiffCell）、冲突横幅（ConflictBanner）、多选 Tab 栏（RenameTabBar）、右侧面板容器（RenamePanel）。这些组件共同构成右侧重命名预览 UI 的视觉骨架。

**Purpose:** 将 D-01~D-05、D-08~D-11、D-13 的决策落地为可视化组件。每个组件独立可测试，后续集成计划只需组合。此计划不修改任何现有文件。

**Output:**
- `DiffCell` — 词段级颜色高亮单元格（新增绿 bg #d4edda / 删除红 bg #f8d7da）
- `ConflictBanner` — 顶部黄色横幅显示冲突计数
- `RenameTabBar` — 6 标签水平多选 Tab 栏（🔢 时间 🔍 正则 📎 前缀 ➕ 后缀 📄 扩展名 🔄 替换）
- `RenamePanel` — 右侧面板容器（空状态 + TabBar + Banner + 表单区域插槽）
</objective>

<execution_context>
@/Users/linuxwps/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/linuxwps/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/02-rename-preview/02-UI-SPEC.md
@/Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/02-rename-preview/02-CONTEXT.md
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/components/FileListRow.tsx
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/components/FileList.css
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/App.css

<interfaces>
<!-- 本计划使用的类型，来自 02-01-PLAN.md 创建的 types/rename.ts -->

export type TabKey = "sequential" | "regex" | "prefix" | "suffix" | "extension" | "replace";

// RenamePanel 接收的 props:
interface RenamePanelProps {
  files: FileItem[];
  modeStates: RenameModeStates;
  onToggleMode: (mode: TabKey) => void;
  onUpdateModeConfig: <K extends TabKey>(mode: K, patch: Partial<RenameModeStates[K]>) => void;
  activeTabForm: TabKey | null;
  onSetActiveTabForm: (tab: TabKey | null) => void;
  totalConflicts: number;
}

// RenameTabBar 接收的 props:
interface RenameTabBarProps {
  modeStates: RenameModeStates;
  onToggleMode: (mode: TabKey) => void;
  activeTabForm: TabKey | null;
  onSetActiveTabForm: (tab: TabKey | null) => void;
  hasFiles: boolean;
}

// ConflictBanner 接收的 props:
interface ConflictBannerProps {
  totalConflicts: number;
}

// DiffCell 接收的 props:
interface DiffCellProps {
  baseNameSegments: DiffSegment[];
  extensionSegments: DiffSegment[];
  hasConflict: boolean;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 DiffCell 差异高亮单元格组件</name>
  <files>
    rname/src/components/rename/DiffCell.tsx
    rname/src/components/rename/DiffCell.css
  </files>
  <read_first>
    .planning/phases/02-rename-preview/02-UI-SPEC.md — DiffCell 组件 contract（Props、CSS 类名、颜色值、冲突样式）
    .planning/phases/02-rename-preview/02-RESEARCH.md — Pattern 3 (DiffCell 代码 + DiffCell.css)
    .planning/phases/02-rename-preview/02-CONTEXT.md — D-02（词段级颜色高亮）、D-03（扩展名和文件名分别高亮）、D-10（冲突行红色高亮+⚠）
  </read_first>
  <action>
    **DiffCell.tsx：**
    - 创建目录 rname/src/components/rename/（如果不存在）
    - 导入 DiffSegment from "../../types/rename"
    - 导入 "./DiffCell.css"
    - 定义接口 DiffCellProps（baseNameSegments: DiffSegment[], extensionSegments: DiffSegment[], hasConflict: boolean）
    - 导出函数组件 `export function DiffCell(props: DiffCellProps)`
    - 渲染一个 `<span className={"diff-cell" + (hasConflict ? " diff-conflict" : "")}>`
    - 如果 hasConflict 为 true，在开头渲染 `<span className="conflict-icon">⚠</span>`
    - 遍历 baseNameSegments，每个 seg 生成 `<span key={"b-" + i} className={"diff-" + seg.type}>{seg.value}</span>`
    - 遍历 extensionSegments，每个 seg 生成 `<span key={"e-" + i} className={"diff-" + seg.type}>{seg.value}</span>`

    **DiffCell.css：**
    - .diff-added: background-color #d4edda, color #155724, border-radius 2px
    - .diff-removed: background-color #f8d7da, color #721c24, border-radius 2px, text-decoration line-through
    - .diff-cell: 行内块级显示
    - .diff-conflict: background-color #fff0f0, border 1px solid #ff4444, border-radius 4px, padding 2px 4px
    - .conflict-icon: margin-right 4px, color #d32f2f

    严格按 UI-SPEC.md 中的颜色值使用，不偏离。
  </action>
  <acceptance_criteria>
    <code>DiffCell.tsx 导出一个函数组件 DiffCell，正确渲染 DiffSegment[]</code>
    <code>DiffCell.css 包含 .diff-added, .diff-removed, .diff-conflict, .conflict-icon 四个 CSS 类</code>
    <code>冲突时 DiffCell 渲染 ⚠ 图标</code>
    <behavior>JSX 使用 key={"b-" + i} 和 key={"e-" + i} 区分 baseName 和 extension 段</behavior>
    <test-command>npm run build 通过</test-command>
  </acceptance_criteria>
  <verify>
    <automated>cd rname && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>DiffCell 组件和 CSS 创建完成，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 ConflictBanner 和 RenameTabBar 组件</name>
  <files>
    rname/src/components/rename/ConflictBanner.tsx
    rname/src/components/rename/ConflictBanner.css
    rname/src/components/rename/RenameTabBar.tsx
    rname/src/components/rename/RenameTabBar.css
  </files>
  <read_first>
    .planning/phases/02-rename-preview/02-UI-SPEC.md — ConflictBanner 设计（颜色 #fff3cd，文本格式）和 Tab 栏交互设计（多选模型，样式规范）
    .planning/phases/02-rename-preview/02-RESEARCH.md — Pattern 4 (Multi-Select Tab Bar) 状态模式
    .planning/phases/02-rename-preview/02-CONTEXT.md — D-04（顶部 Tab 栏）、D-05（单字+图标标签）、D-09（横幅冲突计数）、D-13（Tab 亮起表示启用）
  </read_first>
  <action>
    **ConflictBanner.tsx：**
    - 定义 props 接口: `{ totalConflicts: number }`
    - 导出函数组件，当 totalConflicts &lt;= 0 时返回 null（不渲染）
    - 渲染 `<div className="conflict-banner">⚠️ 发现 {totalConflicts} 个重名文件，请调整重命名规则</div>`

    **ConflictBanner.css：**
    - .conflict-banner: background-color #fff3cd, border 1px solid #ffc107, border-radius 6px, padding 12px 32px, margin-bottom 16px, font-size 14px, color #856404

    **RenameTabBar.tsx：**
    - 导入 RenameModeStates from "../../types/rename"
    - 定义 TabKey 类型: "sequential" | "regex" | "prefix" | "suffix" | "extension" | "replace"
    - 定义 props 接口: `{ modeStates: RenameModeStates; onToggleMode: (mode: TabKey) => void; activeTabForm: TabKey | null; onSetActiveTabForm: (tab: TabKey | null) => void; hasFiles: boolean }`

    - 在组件内部定义 TABS 常量数组（组件外常量，避免每次渲染重建）:
      `const TABS: { key: TabKey; label: string; icon: string }[] = [...]`
      按顺序: sequential "🔢 时间", regex "🔍 正则", prefix "📎 前缀", suffix "➕ 后缀", extension "📄 扩展名", replace "🔄 替换"

    - 渲染一个 `<div className="rename-tab-bar">` 包含 6 个 `<button>`
    - 每个按钮: key=tab.key, className 根据子组件状态动态计算
    - 按钮 class: "rename-tab" + (modeStates[tab.key].enabled ? " tab-active" : "") + (activeTabForm === tab.key ? " tab-form-open" : "")
    - 按钮内容: tab.icon + " " + tab.label
    - 按钮 onClick: 先调用 onToggleMode(tab.key)（切换启用/禁用），然后调用 onSetActiveTabForm(tab.key)（无论启用还是禁用都设为当前打开的表单）
    - 如果 !hasFiles，按钮添加 className "tab-disabled" 并设置 disabled 属性

    **RenameTabBar.css：**
    - .rename-tab-bar: display flex, gap 0, border-bottom 1px solid #eee（当无启用模式时）或 border-bottom 1px dashed #ddd（当有启用模式时——通过额外 className）
    - .rename-tab: padding 8px 16px, font-size 13px, border none, background transparent, cursor pointer, color #666, border-bottom 3px solid transparent, transition all 0.2s
    - .rename-tab.tab-active: color #007AFF, border-bottom 3px solid #007AFF, font-weight 600
    - .rename-tab.tab-form-open: background-color #f5f5f5（表单项打开时背景色提示）
    - .rename-tab.tab-disabled: color #ccc, cursor not-allowed
    - .rename-tab:hover: background-color #f0f0f0
  </action>
  <acceptance_criteria>
    <code>ConflictBanner.tsx 在 totalConflicts <= 0 时返回 null</code>
    <code>RenameTabBar.tsx 使用常量数组 TABS 渲染 6 个按钮，不硬编码 JSX</code>
    <code>RenameTabBar 按钮的 className 根据 enabled + activeTabForm 动态计算</code>
    <code>RenameTabBar 在 !hasFiles 时按钮被 disabled</code>
    <behavior>Tab 样式区分三种状态：未启用（#666）、已启用（#007AFF + 蓝色底边）、表单打开中（#f5f5f5 背景）</behavior>
    <behavior>ConflictBanner 在 totalConflicts=0 时 DOM 中不出现</behavior>
    <test-command>npm run build 通过</test-command>
  </acceptance_criteria>
  <verify>
    <automated>cd rname && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>ConflictBanner 和 RenameTabBar 组件创建完成，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 3: 创建 RenamePanel 右侧面板容器组件</name>
  <files>
    rname/src/components/rename/RenamePanel.tsx
    rname/src/components/rename/RenamePanel.css
  </files>
  <read_first>
    .planning/phases/02-rename-preview/02-UI-SPEC.md — 右侧面板布局（RenamePanel 架构图）、空状态设计、冲突横幅位置
    .planning/phases/02-rename-preview/02-CONTEXT.md — D-08（无文件时显示"请先在左侧添加文件"）
    rname/src/App.css — 现有的 .placeholder 样式参考
  </read_first>
  <action>
    **RenamePanel.tsx：**
    - 导入 RenameModeStates, FileItem from 对应路径
    - 导入 ConflictBanner, RenameTabBar from "./"
    - 导入 "./RenamePanel.css"

    - 定义 TabKey 类型（同 RenameTabBar）
    - 定义 RenamePanelProps:
      ```
      interface RenamePanelProps {
        files: FileItem[];
        modeStates: RenameModeStates;
        onToggleMode: (mode: TabKey) => void;
        onUpdateModeConfig: <K extends TabKey>(mode: K, patch: Partial<RenameModeStates[K]>) => void;
        activeTabForm: TabKey | null;
        onSetActiveTabForm: (tab: TabKey | null) => void;
        totalConflicts: number;
        activeFormComponent?: React.ReactNode;  // 当前活动的表单组件（由父级传入）
      }
      ```

    - 导出函数组件 RenamePanel:
      - 如果 files.length === 0，渲染空状态: `<div className="rename-panel-empty">请先在左侧添加文件</div>`（居中显示 #999 文字）
      - 否则渲染:
        ```
        <div className="rename-panel">
          <ConflictBanner totalConflicts={totalConflicts} />
          <RenameTabBar ...props... />
          <div className="rename-form-area">
            {activeFormComponent || <p className="rename-form-hint">选择一个模式开始重命名</p>}
          </div>
        </div>
        ```

    **RenamePanel.css：**
    - .rename-panel: display flex, flex-direction column, height 100%, padding 0（继承父容器 App.css 的 .right-panel padding）
    - .rename-panel-empty: display flex, align-items center, justify-content center, height 100%, color #999, font-size 14px（复用 App.css 中 .placeholder 的样式风格）
    - .rename-form-area: padding 16px 0, flex 1, overflow-y auto（表单区域可滚动）
    - .rename-form-hint: color #999, font-size 13px, text-align center, padding 24px（无模式选择时的提示文字）
  </action>
  <acceptance_criteria>
    <code>RenamePanel 在 files.length === 0 时渲染空状态"请先在左侧添加文件"</code>
    <code>RenamePanel 渲染 ConflictBanner + RenameTabBar + 表单区域</code>
    <code>表单区域包含插槽 activeFormComponent 用于显示当前活动表单</code>
    <behavior>空状态文本居中显示，样式与 App.css 的 .placeholder 一致（#999, 14px）</behavior>
    <behavior>ConflictBanner 仅在 totalConflicts > 0 时出现</behavior>
    <test-command>npm run build 通过</test-command>
  </acceptance_criteria>
  <verify>
    <automated>cd rname && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>RenamePanel 组件创建完成，TypeScript 编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 组件渲染层 | UI 组件仅接收 props，不处理用户输入数据。所有数据由父级通过 props 传入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | RenameTabBar | accept | Tab 标签为硬编码常量，用户不可修改。不存在注入风险 |
| T-02-05 | Information Disclosure | DiffCell | accept | 差异高亮仅在本地渲染。文件名数据来自父级 props，不持久化、不发送网络 |
</threat_model>

<verification>
1. npm run build 通过，无 TypeScript 错误
2. 4 个组件文件 + 4 个 CSS 文件全部存在
3. DiffCell 正确渲染 added/removed/unchanged 三种状态
4. ConflictBanner 在冲突数为 0 时隐藏
5. RenameTabBar 正确管理 6 标签的启用/禁用状态
6. RenamePanel 正确处理 files.length === 0 的空状态
</verification>

<success_criteria>
1. ✅ DiffCell 支持三种颜色高亮 + 冲突 ⚠ 图标
2. ✅ ConflictBanner 自动显隐（totalConflicts > 0 时显示）
3. ✅ RenameTabBar 实现多选切换（点击切换 enabled）
4. ✅ RenamePanel 空状态提示 + 表单区域插槽就绪
5. ✅ 所有 CSS 类名与 UI-SPEC 颜色规范一致
</success_criteria>

<output>
Plan 完成后的 SUMMARY 文件: `.planning/phases/02-rename-preview/02-02-SUMMARY.md`
</output>
