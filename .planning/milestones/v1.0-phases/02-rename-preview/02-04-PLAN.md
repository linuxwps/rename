---
phase: 02-rename-preview
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01-PLAN.md
  - 02-02-PLAN.md
  - 02-03-PLAN.md
files_modified:
  - rname/src/App.tsx
  - rname/src/components/FileDropZone.tsx
  - rname/src/components/FileList.tsx
  - rname/src/components/FileListRow.tsx
  - rname/src/App.css
  - rname/src/components/FileList.css
autonomous: true
requirements:
  - PREV-01
  - PREV-02
  - PREV-03
  - SEQ-01
  - SEQ-02
  - SEQ-03
  - SEQ-04
  - REGE-01
  - REGE-02
  - REGE-03
  - PREF-01
  - PREF-02
  - SUFF-01
  - SUFF-02
  - EXT-01
  - EXT-02
  - EXT-03
  - REPL-01
  - REPL-02
  - REPL-03
user_setup: []

must_haves:
  truths:
    - "FileList 表格新增'新文件名'列，显示实时预览结果"
    - "预览数据通过 App.tsx 从右侧面板流向左面板 FileList"
    - "用户切换 Tab 时保持表单输入内容"
    - "300ms 防抖后预览自动刷新"
    - "重命名预览列显示高亮差异"
    - "冲突行显示红色高亮和 ⚠ 图标"
    - "使用 6 种模式可以叠加组合"
    - "所有 20 个需求的功能端到端可用"
  artifacts:
    - path: "rname/src/App.tsx"
      provides: "状态根：useFileList + useRenameEngine + modeStates 管理"
      contains: "useRenameEngine(files, debouncedModes)"
    - path: "rname/src/components/FileDropZone.tsx"
      provides: "接收 files/previews 等 props，替换内部 useFileList 调用"
      contains: "interface FileDropZoneProps"
    - path: "rname/src/components/FileList.tsx"
      provides: "接收 previews prop，渲染'新文件名'列"
      contains: "previews?: Map&lt;string, PreviewResult&gt;"
    - path: "rname/src/components/FileListRow.tsx"
      provides: "使用 DiffCell 渲染新文件名，冲突行样式"
      contains: "DiffCell"
  key_links:
    - from: "App.tsx"
      to: "hooks/useRenameEngine.ts"
      via: "调用 useRenameEngine(files, debouncedModes)"
    - from: "App.tsx"
      to: "components/rename/RenamePanel.tsx"
      via: "导入并渲染 RenamePanel，传递 props"
    - from: "App.tsx"
      to: "components/FileDropZone.tsx"
      via: "传递 files, previews 等 props"
    - from: "FileList.tsx"
      to: "FileListRow.tsx"
      via: "传递 previewResult prop"
    - from: "FileListRow.tsx"
      to: "DiffCell.tsx"
      via: "渲染 DiffCell 替代纯文本文件名"
---

<objective>
**目标：** 将所有部件集成到完整系统中。状态提升：useFileList 从 FileDropZone 上移到 App.tsx，useRenameEngine 在 App.tsx 中实例化，预览数据通过 props 流向左右两个面板。右侧 RenamePanel 激活后替换 placeholder。

**Purpose:** 将前三个计划创建的独立组件连接为工作系统。此计划完成后，Phase 2 的 20 个需求全部实现。

**Output:** 6 个现有文件被修改
</objective>

<execution_context>
@/Users/linuxwps/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/linuxwps/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/02-rename-preview/02-CONTEXT.md
@/Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/02-rename-preview/02-UI-SPEC.md
@/Users/linuxwps/Downloads/OpenCode/Rname/.planning/phases/02-rename-preview/02-RESEARCH.md
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/App.tsx
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/components/FileDropZone.tsx
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/components/FileList.tsx
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/components/FileListRow.tsx
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/App.css
@/Users/linuxwps/Downloads/OpenCode/Rname/rname/src/components/FileList.css

<interfaces>
<!-- 以下类型和组件在 Plan 01~03 中创建，本计划需使用 -->

From useFileList.ts:
  return { files, isDragging, addFiles, removeFile, clearFiles, openFilePicker, openFolderPicker }

From useRenameEngine.ts:
  return { previews: PreviewResult[], totalConflicts: number }

From useDebounce.ts:
  function useDebounce<T>(value: T, delay: number): T

From RenamePanel.tsx:
  interface RenamePanelProps {
    files: FileItem[];
    modeStates: RenameModeStates;
    onToggleMode: (mode: TabKey) => void;
    onUpdateModeConfig: <K extends TabKey>(mode: K, patch: Partial<RenameModeStates[K]>) => void;
    activeTabForm: TabKey | null;
    onSetActiveTabForm: (tab: TabKey | null) => void;
    totalConflicts: number;
    activeFormComponent?: React.ReactNode;
  }

From DiffCell.tsx:
  interface DiffCellProps {
    baseNameSegments: DiffSegment[];
    extensionSegments: DiffSegment[];
    hasConflict: boolean;
  }

Form components:
  SequentialForm({ config: SequentialConfig, onChange })
  RegexForm({ config: RegexConfig, onChange })
  PrefixForm({ config: PrefixConfig, onChange })
  SuffixForm({ config: SuffixConfig, onChange })
  ExtensionForm({ config: ExtensionConfig, onChange })
  ReplaceForm({ config: ReplaceConfig, onChange })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 状态提升到 App.tsx — 集成 useFileList + useRenameEngine + RenamePanel</name>
  <files>
    rname/src/App.tsx
    rname/src/components/FileDropZone.tsx
  </files>
  <read_first>
    rname/src/App.tsx — 当前结构（左右面板，右面板 placeholder）
    rname/src/hooks/useFileList.ts — 完整导出接口
    rname/src/hooks/useRenameEngine.ts — 本计划依赖的 hook 签名
    rname/src/hooks/useDebounce.ts — 本计划依赖的 hook
    rname/src/types/rename.ts — RenameModeStates 等类型
    rname/src/components/rename/RenamePanel.tsx — RenamePanelProps
    rname/src/components/rename/SequentialForm.tsx — SequentialFormProps
    rname/src/components/rename/RegexForm.tsx — RegexFormProps
    rname/src/components/rename/PrefixForm.tsx — PrefixFormProps
    rname/src/components/rename/SuffixForm.tsx — SuffixFormProps
    rname/src/components/rename/ExtensionForm.tsx — ExtensionFormProps
    rname/src/components/rename/ReplaceForm.tsx — ReplaceFormProps
    rname/src/components/FileDropZone.tsx — 当前实现（内部调用 useFileList）
  </read_first>
  <action>
    1.1 修改 App.tsx：
    - 删除原有的 import { FileDropZone } 和单一渲染
    - 添加以下 import：
      ```
      import { useState, useCallback, useMemo } from "react";
      import { useFileList } from "./hooks/useFileList";
      import { useRenameEngine } from "./hooks/useRenameEngine";
      import { useDebounce } from "./hooks/useDebounce";
      import { FileDropZone } from "./components/FileDropZone";
      import { RenamePanel } from "./components/rename/RenamePanel";
      import { SequentialForm } from "./components/rename/SequentialForm";
      import { RegexForm } from "./components/rename/RegexForm";
      import { PrefixForm } from "./components/rename/PrefixForm";
      import { SuffixForm } from "./components/rename/SuffixForm";
      import { ExtensionForm } from "./components/rename/ExtensionForm";
      import { ReplaceForm } from "./components/rename/ReplaceForm";
      import type { RenameModeStates } from "./types/rename";
      import "./App.css";
      ```

    - 在 App 组件函数体内：
      a) 调用 useFileList() 获取 { files, isDragging, addFiles, removeFile, clearFiles, openFilePicker, openFolderPicker }

      b) 定义 modeStates 的初始值（useState），严格按 UI-SPEC.md 默认值：
        ```
        const [modeStates, setModeStates] = useState<RenameModeStates>({
          sequential: { enabled: false, startAt: 1, digits: 3, position: "after" },
          regex: { enabled: false, pattern: "", replacement: "", caseSensitive: false },
          prefix: { enabled: false, text: "" },
          suffix: { enabled: false, text: "" },
          extension: { enabled: false, mode: "change", newExtension: "" },
          replace: { enabled: false, findText: "", replaceText: "", caseSensitive: false },
        });
        ```

      c) 定义 activeTabForm 状态（useState<TabKey | null>），初始为 null
        ```
        const [activeTabForm, setActiveTabForm] = useState<TabKey | null>(null);
        type TabKey = "sequential" | "regex" | "prefix" | "suffix" | "extension" | "replace";
        ```

      d) 定义 onToggleMode 回调（useCallback）:
        切换指定 mode 的 enabled 状态: `setModeStates(prev => ({ ...prev, [mode]: { ...prev[mode], enabled: !prev[mode].enabled } }))`

      e) 定义 onUpdateModeConfig 回调（useCallback）:
        更新指定 mode 的部分配置: `setModeStates(prev => ({ ...prev, [mode]: { ...prev[mode], ...patch } }))`

      f) 调用 useDebounce(modeStates, 300) 获取 debouncedModes（D-07: 300ms 防抖）

      g) 调用 useRenameEngine(files, debouncedModes) 获取 { previews, totalConflicts }

      h) 将 previews 数组转为 Map&lt;string, PreviewResult&gt; 方便 FileList 根据 fileId 查找: `const previewMap = useMemo(() => new Map(previews.map(p => [p.fileId, p])), [previews])`

      i) 计算 activeFormComponent（useMemo，依赖 activeTabForm + modeStates + onUpdateModeConfig）:
        根据 activeTabForm 返回相应的表单组件: sequential → SequentialForm, regex → RegexForm, 等
        如果 activeTabForm 为 null，返回 undefined（RenamePanel 将显示 hint）

      j) JSX 渲染：
        ```
        <div className="app">
          <div className="left-panel">
            <FileDropZone
              files={files}
              isDragging={isDragging}
              previews={previewMap}
              onRemoveFile={removeFile}
              onClearFiles={clearFiles}
              onOpenFilePicker={openFilePicker}
              onOpenFolderPicker={openFolderPicker}
            />
          </div>
          <div className="right-panel">
            <RenamePanel
              files={files}
              modeStates={modeStates}
              onToggleMode={onToggleMode}
              onUpdateModeConfig={onUpdateModeConfig}
              activeTabForm={activeTabForm}
              onSetActiveTabForm={setActiveTabForm}
              totalConflicts={totalConflicts}
              activeFormComponent={activeFormComponent}
            />
          </div>
        </div>
        ```

    1.2 修改 FileDropZone.tsx：
    - 删除 import { useFileList }
    - 如果需要导入 FileItem, PreviewResult 类型
    - 修改接口:
      ```
      import type { FileItem } from "../types/file";
      import type { PreviewResult } from "../types/rename";
      
      interface FileDropZoneProps {
        files: FileItem[];
        isDragging: boolean;
        previews: Map<string, PreviewResult>;
        onRemoveFile: (fileId: string) => void;
        onClearFiles: () => void;
        onOpenFilePicker: () => void;
        onOpenFolderPicker: () => void;
      }
      ```
    - 组件参数改为从 props 接收
    - 渲染 `<FileList files={files} previews={previews} onRemoveFile={onRemoveFile} onClearFiles={onClearFiles} />`
    - 空状态（files.length === 0）渲染保持不变（拖拽区域 + 按钮）
    - 非空状态渲染：`<FileList .../>`（之前是通过 useFileList 内部获取数据，现在通过 props 传入）
  </action>
  <acceptance_criteria>
    <code>App.tsx 不再硬编码 placeholder，而是渲染 RenamePanel 组件</code>
    <code>App.tsx 调用 useFileList() 和 useRenameEngine() 两个 hook</code>
    <code>useDebounce(modeStates, 300) 实现 D-07 防抖</code>
    <code>FileDropZone 不再调用 useFileList，改为通过 props 接收所有数据</code>
    <code>App.tsx 中 modeStates 初始值严格按 UI-SPEC 默认值</code>
    <behavior>npm run build 无类型错误</behavior>
    <test-command>npm run build 通过</test-command>
  </acceptance_criteria>
  <verify>
    <automated>cd rname && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>App.tsx 状态提升完成，FileDropZone 转 props 驱动</done>
</task>

<task type="auto">
  <name>Task 2: 修改 FileList 和 FileListRow 添加预览列和 DiffCell</name>
  <files>
    rname/src/components/FileList.tsx
    rname/src/components/FileListRow.tsx
  </files>
  <read_first>
    rname/src/components/FileList.tsx — 当前实现（内部调用 useFileList）
    rname/src/components/FileListRow.tsx — 当前实现（渲染表格行）
    rname/src/components/rename/DiffCell.tsx — 本计划 Task 2 创建的组件
    rname/src/types/rename.ts — PreviewResult 类型
    .planning/phases/02-rename-preview/02-UI-SPEC.md — "新文件名"列在表格中的位置（文件名和扩展名之间）
  </read_first>
  <action>
    2.1 修改 FileList.tsx：
    - 删除 import { useFileList }
    - 添加 import:
      ```
      import type { FileItem } from "../types/file";
      import type { PreviewResult } from "../types/rename";
      import { FileListRow } from "./FileListRow";
      import "./FileList.css";
      ```
    - 定义 props 接口:
      ```
      interface FileListProps {
        files: FileItem[];
        previews: Map<string, PreviewResult>;
        onRemoveFile: (fileId: string) => void;
        onClearFiles: () => void;
      }
      ```
    - 从 props 解构：`export function FileList({ files, previews, onRemoveFile, onClearFiles }: FileListProps)`
    - 表格 <thead> 中在"文件名"列之后插入新的 `<th className="new-name-column">新文件名</th>`
    - 表格 <tbody> 中: `<FileListRow key={file.id} file={file} previewResult={previews.get(file.id)} onRemove={onRemoveFile} />`
    - 清空按钮使用 onClearFiles prop

    2.2 修改 FileListRow.tsx：
    - 添加 import:
      ```
      import type { PreviewResult } from "../types/rename";
      import { DiffCell } from "./rename/DiffCell";
      ```
    - 修改 props 接口: 添加 `previewResult?: PreviewResult`
    - 在 <tr> 上: 添加 className: `"file-row" + (previewResult?.hasConflict ? " conflict-row" : "")`
    - 在文件名列（当前 <td className="file-name">）之后，扩展名列之前插入新列:
      ```
      <td className="new-name-column">
        {previewResult ? (
          <DiffCell
            baseNameSegments={previewResult.diffBaseName}
            extensionSegments={previewResult.diffExtension}
            hasConflict={previewResult.hasConflict}
          />
        ) : (
          <span className="preview-placeholder">—</span>
        )}
      </td>
      ```
    - preview-placeholder: color #ccc, font-size 13px（表示预览尚未计算的占位符）
  </action>
  <acceptance_criteria>
    <code>FileList 接收 files, previews, onRemoveFile, onClearFiles 四个 props</code>
    <code>FileList 表格 thead 中新文件名列在文件名和扩展名列之间</code>
    <code>FileListRow 在 previewResult 存在时使用 DiffCell 渲染新文件名</code>
    <code>FileListRow 中 previewResult 不存在时显示占位符"—"</code>
    <code>冲突行 tr 添加 className "conflict-row"</code>
    <behavior>npm run build 无类型错误</behavior>
    <test-command>npm run build 通过</test-command>
  </acceptance_criteria>
  <verify>
    <automated>cd rname && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>FileList 和 FileListRow 添加预览列完成</done>
</task>

<task type="auto">
  <name>Task 3: 更新 CSS — 添加重命名区域样式 + 新文件名列样式 + 冲突行样式</name>
  <files>
    rname/src/App.css
    rname/src/components/FileList.css
  </files>
  <read_first>
    rname/src/App.css — 当前样式（左右面板布局）
    rname/src/components/FileList.css — 当前表格样式
    .planning/phases/02-rename-preview/02-UI-SPEC.md — 所有颜色值、间距、冲突行样式
  </read_first>
  <action>
    3.1 修改 App.css，在 .right-panel 和 .placeholder 之后添加：
    ```
    /* Phase 2: Rename Panel */
    .rename-tab-bar {
      display: flex;
      gap: 0;
      border-bottom: 1px solid #eee;
      margin-bottom: 16px;
    }
    
    /* Tab 单个样式已在 Plan 02 RenameTabBar.css 中定义 */
    /* 这里仅补充布局级别的样式 */
    
    .rename-panel-empty {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100%;
      color: #999;
      font-size: 14px;
    }
    ```
    （实际上 RenamePanel.css 应已包含 .rename-panel-empty，所以此项可选。但保留 .right-panel 的 padding 不变）

    - 确保 .right-panel 的 padding 16px 保留不变

    3.2 修改 FileList.css，添加：

    a) `.new-name-column` 列样式:
      `width: auto, min-width 150px`（可伸缩列，给足空间显示新文件名 + 高亮）

    b) `.conflict-row`:
      `background-color: #fff0f0`（冲突行背景色, 按 UI-SPEC）

    c) `.conflict-row td`:
      `border-bottom: 1px solid #ffcdd2`（冲突行的行间淡化红色边框）

    d) `.preview-placeholder`:
      `color: #ccc, font-size: 13px`（预览尚未计算时的占位符）

    e) 确保 `.file-name` 的 max-width 不变（原有 200px），新文件名列使用 min-width + overflow:hidden text-overflow:ellipsis 防止过长文件名撑破布局

    f) 如果 table-layout 不是 fixed，考虑添加 `table-layout: fixed` 以控制列宽比例：
       - 文件名列: width 25%
       - 新文件名列: width 30%
       - 扩展名列: width 12%
       - 大小列: width 12%
       - 日期列: width 15%
       - 操作列: width 6%
    （可选，不强制修改 table-layout 以保持现有布局兼容性。如果 table-layout 导致布局溢出，使用 min-width 替代）

    - 注意：不要破坏现有表格样式。仅添加新类和修改必要的宽度属性。
  </action>
  <acceptance_criteria>
    <code>FileList.css 包含 .new-name-column, .conflict-row, .preview-placeholder 类</code>
    <code>App.css 中 .right-panel padding 保持不变（16px）</code>
    <code>冲突行 background-color #fff0f0</code>
    <code>预览占位符 color #ccc</code>
    <style>npm run build 通过，页面样式与 UI-SPEC 一致</style>
    <test-command>npm run build 通过</test-command>
  </acceptance_criteria>
  <verify>
    <automated>cd rname && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>CSS 更新完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx 状态中介 | 所有用户输入和计算状态在此集中管理，再分发到子组件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service | App.tsx (useRenameEngine) | mitigate | useDebounce 300ms 保证用户快速输入不会导致频繁重算。useMemo 缓存计算结果，依赖不变时不重算 |
| T-02-09 | Elevation of Privilege | App.tsx → FileDropZone | accept | 状态提升仅为父子组件通信，不涉及权限边界。Phase 3 执行重命名时才需要关注路径安全 |
</threat_model>

<verification>
1. npm run build 通过，无 TypeScript 错误
2. App.tsx 成功集成 useFileList + useRenameEngine + useDebounce
3. FileDropZone 从 props 驱动（不再调用 useFileList）
4. FileList 展示"新文件名"列，内容来自 PreviewResult
5. FileListRow 使用 DiffCell 渲染高亮差异
6. 冲突行正确显示红色背景 + ⚠ 图标
7. 右侧 RenamePanel 替代旧的 placeholder
8. 6 种模式表单可通过 Tab 切换并使用
</verification>

<success_criteria>
1. ✅ 右侧面板 RenamePanel 激活，Tab 栏可以切换 6 种模式
2. ✅ 切换 Tab 保留输入内容（D-06）
3. ✅ 300ms 防抖正常生效（D-07）
4. ✅ FileList 表格显示"新文件名"列（D-01）
5. ✅ 差异高亮显示（新增绿/删除红）（D-02）
6. ✅ 扩展名和文件名分别高亮（D-03）
7. ✅ 冲突时横幅显示计数（D-09）
8. ✅ 冲突行红色高亮 + ⚠ 图标（D-10）
9. ✅ 无文件时显示"请先在左侧添加文件"（D-08）
10. ✅ 所有 20 个需求的端到端功能可用
</success_criteria>

<output>
Plan 完成后的 SUMMARY 文件: `.planning/phases/02-rename-preview/02-04-SUMMARY.md`
</output>
